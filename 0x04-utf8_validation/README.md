# UTF8 validation
This code checks if UTF8 values are valid.

It receives decimal strings, or UTF8 strings. The code is expected to validated the
strings, by first converting them to bytes then verifying if they are valid encoding.


